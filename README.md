# Simple demmonstration or template regarding a docker orientated rc script

## Implementation

A demo program "fumper" (just writing "Fump" to it's log each second should
simulate a kind of daemon process running inside docker.
